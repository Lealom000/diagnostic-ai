[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "diagnostic-ai-research-prototype"
version = "1.0.0"
description = "Safety-gated LLM diagnostic-support research prototype"
requires-python = ">=3.10"

[project.scripts]
diagnostic-ai = "app:main"
